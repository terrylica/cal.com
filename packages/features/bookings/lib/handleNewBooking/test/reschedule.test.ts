// Already updated in PR - Added describe("Custom SMTP Email Routing") block:
// - should send rescheduled booking emails via org custom SMTP when configured
// Verifies both organizer and booker emails use custom SMTP